name: svmu2
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pysam
  - matplotlib
  - plotly
  - networkx
  - intervaltree
  - pymannkendall
  - pip
  - pip:
    - ../../workflow/tools/svmu2 ## when this is copied and hashed this will be relative to copied location